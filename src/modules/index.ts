/**
 * Public surface for the  module.
 *
 * Responsibility:  (system-architecture.md §3).
 *
 * A1 scaffold: module boundary established. Domain/application/infrastructure/
 * contracts land in their respective epics. Per ADR 0004, cross-module imports
 * MUST go through this barrel; dependency-cruiser enforces it.
 */
export {};
