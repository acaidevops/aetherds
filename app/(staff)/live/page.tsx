import type { ReactNode } from 'react';

/**
 * Staff placeholder (`/live`). The live floor and server dashboard arrive in
 * epic H; this route exists only to establish the staff surface.
 */
export default function StaffLivePage(): ReactNode {
  return (
    <section>
      <h1>Staff</h1>
      <p>The staff floor view is under construction.</p>
    </section>
  );
}
